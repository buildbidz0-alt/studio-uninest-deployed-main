# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

# Add secret environment variables here.
# You will need to create these secrets in Google Cloud Secret Manager.
# https://firebase.google.com/docs/app-hosting/configure#secret-env
secretEnv:
  - secret: SUPABASE_SERVICE_KEY
  - secret: RAZORPAY_KEY_ID
  - secret: RAZORPAY_KEY_SECRET
